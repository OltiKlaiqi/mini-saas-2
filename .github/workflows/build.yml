name: CI/CD Pipeline
 
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
 
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
 
    steps:
    # Checkout source code
    - name: Checkout code
      uses: actions/checkout@v4
 
    # Set up Docker Buildx
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
 
    # Log in to Docker Hub (optional, if pushing)
    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
 
    # Build Docker image
    - name: Build Docker image
      run: docker build -t mini-saas-2 .
 
    # (Optional) Push Docker image to Docker Hub
    - name: Push Docker image
      run: docker tag mini-saas-2 ${{ secrets.DOCKER_USERNAME }}/mini-saas-2:latest && docker push ${{ secrets.DOCKER_USERNAME }}/mini-saas-2:latest
 
    # (Optional) Deploy to remote server via SSH
    - name: Deploy via SSH
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SSH_HOST }}
        username: ${{ secrets.SSH_USER }}
        key: ${{ secrets.SSH_KEY }}
        script: |
          docker pull ${{ secrets.DOCKER_USERNAME }}/mini-saas-2:latest
          docker stop mini-saas || true
          docker rm mini-saas || true
          docker run -d -p 8080:8080 --name mini-saas ${{ secrets.DOCKER_USERNAME }}/mini-saas-2:latest
